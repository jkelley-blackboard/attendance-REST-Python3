# Compatible-release pins: security and bug-fix updates flow through,
# major-version changes do not. Everything else the script imports is
# in the Python 3.11 standard library.
requests~=2.32
python-dotenv~=1.0
